fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic[email]==2.10.4
pydantic-settings==2.7.0
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0
python-multipart==0.0.19
# Replaced unmaintained python-jose (last release 2022; CVE-2024-33663
# algorithm-confusion + CVE-2024-33664 DoS) with PyJWT, which is
# actively maintained and has a smaller, single-purpose attack surface.
PyJWT>=2.8,<3
# Used directly for symmetric Fernet encryption of camera credentials
# at rest. (Was previously transitively pulled in by python-jose; pin
# explicitly so removing jose doesn't break the import.)
cryptography>=42,<46
passlib[bcrypt]==1.7.4
# bcrypt 4.1+ removed the `__about__` attribute that passlib 1.7.4
# probes at import time; the missing attribute can manifest as silent
# password-verify failures on some platforms (locks every admin out
# with NO log line). Pin <4.1 until passlib ships a fix.
bcrypt>=4.0.1,<4.1
numpy==1.26.4
opencv-python==4.10.0.84
onnxruntime==1.20.1
insightface==0.7.3
Pillow==11.0.0
openpyxl==3.1.5
python-dotenv==1.0.1
tzdata==2024.2
scikit-learn>=1.3,<2
slowapi==0.1.9
# HTTP metrics auto-collected by middleware; custom gauges (worker
# pool depths, last frame age, embedding cache size) are emitted via
# the underlying `prometheus_client` gauge primitives.
prometheus-fastapi-instrumentator>=7,<9
