# Dev / test dependencies. Kept separate from `requirements.txt` so
# production images don't carry test infrastructure.
-r requirements.txt
pytest>=8.3,<10
pytest-asyncio>=0.23,<2
pytest-cov>=5.0,<7
pytest-mock>=3.14,<4
freezegun>=1.5,<2
httpx>=0.27,<0.28
# Lint + format. One tool, fast, deterministic. Replaces black/isort/flake8.
ruff>=0.7,<1
# Static type checker. Pyright is faster than mypy for our codebase
# size and has better support for SQLAlchemy 2 / Pydantic v2 typing.
pyright>=1.1.380,<2
